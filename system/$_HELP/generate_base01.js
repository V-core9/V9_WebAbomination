const v_scrolls = require('v_scrolls');

// -> Use this as template for the generator config file.
const files_export_list = require('./readme_base01');

// -> Run it.
v_scrolls.generate(files_export_list);
